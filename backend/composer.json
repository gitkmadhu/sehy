{
    "name": "sehy/backend",
    "description": "Sehy classifieds PHP/MySQL API — no Composer packages used, this file exists so buildpack-based hosts (e.g. DigitalOcean App Platform) detect this directory as a PHP app and provision the right runtime/extensions.",
    "require": {
        "php": ">=8.0",
        "ext-pdo": "*",
        "ext-pdo_mysql": "*",
        "ext-gd": "*",
        "ext-curl": "*",
        "ext-json": "*",
        "ext-exif": "*"
    }
}
